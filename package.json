{
  "name": "linkedin-personal-branding-mcp",
  "version": "0.2.0",
  "private": true,
  "description": "Local MCP server that reads a LinkedIn profile with Playwright and exports structured JSON for personal branding workflows.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "login": "tsx src/login.ts",
    "extract": "tsx src/extract-cli.ts",
    "compare": "tsx src/compare-exports.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "dotenv": "^16.6.1",
    "playwright": "^1.59.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
